{
  "name": "job_surfers",
  "version": "1.0.0",
  "description": "job_surfers",
  "main": "common.js",
  "scripts": {
    "build": "node build.js",
    "scrape": "S3_BUCKET=careers-surf-data node scrape_jobs.js --pages 1",
    "scrape:city": "S3_BUCKET=careers-surf-data node scrape_jobs.js --pages 1 --city",
    "scrape:local": "node scrape_jobs.js --pages 1",
    "scrape:local:city": "node scrape_jobs.js --pages 1 --city",
    "upload": "S3_BUCKET=careers-surf-data node upload_jobs.js",
    "upload:cities": "S3_BUCKET=careers-surf-data node upload_cities.js",
    "upload:city": "S3_BUCKET=careers-surf-data node upload_cities.js --city",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1024.0",
    "puppeteer": "^24.39.1",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  }
}
